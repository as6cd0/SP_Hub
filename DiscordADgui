local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
local API_URL = "http://78.154.103.18:11336/check_whitelist"

local COUNTDOWN_SECONDS = 10
local HTTP_TIMEOUT_SECONDS = 2.5
local CACHE_TTL_SECONDS = 60
local DISCORD_LINK = "https://discord.gg/6Yp2B4JHyz"

local cache = {
    result = nil,
    status = nil,
    time = 0
}

local function notify(title, text, duration)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = duration or 3
        })
    end)
end

local HttpRequest = (syn and syn.request) 
    or (http and http.request) 
    or request 
    or http_request 
    or (fluxus and fluxus.request)

local function fastHttpGet(url, timeout)
    timeout = timeout or HTTP_TIMEOUT_SECONDS

    if HttpRequest then
        local ok, res = pcall(function()
            return HttpRequest({
                Url = url,
                Method = "GET",
                Timeout = timeout,
                Headers = {
                    ["User-Agent"] = "SP-Hub-Client/1.0",
                    ["Accept"] = "application/json"
                }
            })
        end)

        if ok and res then
            local body = (type(res) == "table" and res.Body) or (type(res) == "string" and res) or nil
            local statusCode = (type(res) == "table" and (res.StatusCode or res.Status)) or 200
            if body and (statusCode == 200 or statusCode == 0) then
                return true, body
            end
        end
    end

    if HttpService.HttpEnabled then
        local ok, res = pcall(function()
            return HttpService:GetAsync(url, false)
        end)
        if ok and type(res) == "string" and res ~= "" then
            return true, res
        end
    end

    return false, nil
end

local function isWhitelisted()
    local username = player and player.Name or ""
    if username == "" then
        return "offline", false
    end

    if cache.result ~= nil and (tick() - cache.time) < CACHE_TTL_SECONDS then
        return cache.status or "ok", cache.result
    end

    local url = API_URL .. "?username=" .. HttpService:UrlEncode(username)
    local ok, body = fastHttpGet(url, HTTP_TIMEOUT_SECONDS)

    if not ok or not body then
        cache.result = false
        cache.status = "offline"
        cache.time = tick()
        return "offline", false
    end

    local decoded = nil
    local success = pcall(function()
        decoded = HttpService:JSONDecode(body)
    end)

    if not success or type(decoded) ~= "table" then
        cache.result = false
        cache.status = "offline"
        cache.time = tick()
        return "offline", false
    end

    local is_wl = (decoded.whitelisted == true)
    cache.result = is_wl
    cache.status = "ok"
    cache.time = tick()
    return "ok", is_wl
end

local ServerLeftNotice = {
    en = "You left our Discord server or you are not registered! Join our server to remove ads!",
    ar = "انت خرجت من سيرفر الديسكورد بتاعنا أو مش متفعل! ادخل السيرفر عشان تشغل السكربت من غير إعلانات",
    es = "¡Saliste de nuestro servidor de Discord o no estás registrado! ¡Únete para quitar los anuncios!",
    tr = "Discord sunucumuzdan ayrıldın veya kayıtlı değilsin! Reklamları kaldırmak için katıl!",
    ru = "Ты вышел из нашего Discord сервера или не зарегистрирован! Зайди в сервер, чтобы убрать рекламу!",
    fr = "Tu as quitté notre serveur Discord ou tu n'es pas inscrit ! Rejoins-nous pour enlever la pub !",
    de = "Du hast unseren Discord Server verlassen oder bist nicht registriert! Tritt bei, um Werbung zu entfernen!",
    pt = "Você saiu do nosso servidor do Discord ou não está registrado! Entre para remover os anúncios!"
}

local function getSavedLanguageCode()
    local code = getgenv and getgenv().SP_Hub_Language
    if code then return code end
    code = "en"
    pcall(function()
        if readfile and isfile and isfile("dropdowns.json") then
            local data = readfile("dropdowns.json")
            if data then
                local json = HttpService:JSONDecode(data)
                local savedName = json and json["LanguageSetting"]
                if savedName then
                    local Codes = {
                        English = "en",
                        Arabic = "ar",
                        ["Española"] = "es",
                        Turkey = "tr",
                        Russian = "ru",
                        France = "fr",
                        Germany = "de",
                        Brazil = "pt"
                    }
                    code = Codes[savedName] or "en"
                end
            end
        end
    end)
    return code
end

notify("Checking", "Wait for checking if you are registered or not...", 1.5)

local status, whitelisted = isWhitelisted()

if status == "ok" and whitelisted then
    notify("✅ Registered", "You are registered! Enjoy using SP Hub", 3)
    return
elseif status == "offline" then
    notify("⚠️ Server Offline", "Server is currently offline. Ad will show until server reconnects.", 3)
else
    local langCode = getSavedLanguageCode()
    local noticeText = ServerLeftNotice[langCode] or ServerLeftNotice["en"]
    notify("❌ Not Registered", noticeText, 5)
end

local parentGui = (game:GetService("CoreGui")) or (player and player:FindFirstChild("PlayerGui"))
if not parentGui then return end

local existingGui = parentGui:FindFirstChild("DiscordAdUI")
if existingGui then
    pcall(function() existingGui:Destroy() end)
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DiscordAdUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = parentGui

local blurInstance = Lighting:FindFirstChild("AdUI_Pro_Blur")
if not blurInstance then
    blurInstance = Instance.new("BlurEffect")
    blurInstance.Name = "AdUI_Pro_Blur"
    blurInstance.Parent = Lighting
    blurInstance.Size = 0
end

local function removeBlur()
    pcall(function()
        local b = Lighting:FindFirstChild("AdUI_Pro_Blur")
        if b then b:Destroy() end
    end)
end

ScreenGui.AncestryChanged:Connect(function(_, parent)
    if not parent then
        removeBlur()
    end
end)

local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Parent = ScreenGui
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
mainFrame.Size = UDim2.new(0, 560, 0, 320)
mainFrame.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true

local mainCorner = Instance.new("UICorner", mainFrame)
mainCorner.CornerRadius = UDim.new(0, 12)

local stroke = Instance.new("UIStroke", mainFrame)
stroke.Color = Color3.fromRGB(45, 45, 45)
stroke.Thickness = 1.4

local header = Instance.new("TextLabel", mainFrame)
header.Size = UDim2.new(1, 0, 0, 52)
header.Position = UDim2.new(0, 0, 0, 0)
header.BackgroundTransparency = 1
header.Text = "SP HUB - AD"
header.TextColor3 = Color3.fromRGB(255, 255, 255)
header.TextScaled = true
header.Font = Enum.Font.GothamBold

local currentLang = getSavedLanguageCode()
local info = Instance.new("TextLabel", mainFrame)
info.Size = UDim2.new(0.92, 0, 0, 36)
info.Position = UDim2.new(0.04, 0, 0, 62)
info.BackgroundTransparency = 1
if currentLang == "ar" then
    info.Text = "ادخل سيرفر الديسكورد لتفعيل السكربت وإزالة الإعلانات"
else
    info.Text = "Join our Discord server to get verified and remove this ad"
end
info.TextColor3 = Color3.fromRGB(200, 200, 200)
info.TextScaled = true
info.Font = Enum.Font.Gotham

local progressOuter = Instance.new("Frame", mainFrame)
progressOuter.Size = UDim2.new(0.88, 0, 0, 18)
progressOuter.Position = UDim2.new(0.06, 0, 0, 120)
progressOuter.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
progressOuter.BorderSizePixel = 0
local progressCorner = Instance.new("UICorner", progressOuter)
progressCorner.CornerRadius = UDim.new(0, 9)

local progressInner = Instance.new("Frame", progressOuter)
progressInner.Name = "ProgressInner"
progressInner.Size = UDim2.new(1, 0, 1, 0)
progressInner.Position = UDim2.new(0, 0, 0, 0)
progressInner.BackgroundColor3 = Color3.fromRGB(255, 215, 120)
local innerCorner = Instance.new("UICorner", progressInner)
innerCorner.CornerRadius = UDim.new(0, 9)

local secondsLabel = Instance.new("TextLabel", mainFrame)
secondsLabel.Size = UDim2.new(0.3, 0, 0, 36)
secondsLabel.Position = UDim2.new(0.35, 0, 0, 148)
secondsLabel.BackgroundTransparency = 1
secondsLabel.Text = tostring(COUNTDOWN_SECONDS)
secondsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
secondsLabel.TextScaled = true
secondsLabel.Font = Enum.Font.GothamBold

local copyButton = Instance.new("TextButton", mainFrame)
copyButton.Name = "CopyButton"
copyButton.Size = UDim2.new(0, 220, 0, 48)
copyButton.Position = UDim2.new(0.5, -110, 0, 196)
copyButton.BackgroundColor3 = Color3.fromRGB(34, 34, 34)
copyButton.BorderSizePixel = 0
copyButton.Text = "Copy Discord Link"
copyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
copyButton.TextScaled = true
copyButton.Font = Enum.Font.GothamBold
local copyCorner = Instance.new("UICorner", copyButton)
copyCorner.CornerRadius = UDim.new(0, 8)
local copyStroke = Instance.new("UIStroke", copyButton)
copyStroke.Color = Color3.fromRGB(60, 60, 60)
copyStroke.Thickness = 1

copyButton.MouseEnter:Connect(function()
    pcall(function()
        TweenService:Create(copyButton, TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = Color3.fromRGB(44, 44, 44)}):Play()
    end)
end)
copyButton.MouseLeave:Connect(function()
    pcall(function()
        TweenService:Create(copyButton, TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = Color3.fromRGB(34, 34, 34)}):Play()
    end)
end)

if player and (player.Name == "nadermohamedtest" or player.Name == "nadermohamed6" or player.Name == "hhnnader2") then
    local closeButton = Instance.new("TextButton", mainFrame)
    closeButton.AnchorPoint = Vector2.new(1, 1)
    closeButton.Position = UDim2.new(1, -16, 1, -16)
    closeButton.Size = UDim2.new(0, 110, 0, 40)
    closeButton.BackgroundColor3 = Color3.fromRGB(170, 40, 40)
    closeButton.BorderSizePixel = 0
    closeButton.Text = "Close"
    closeButton.TextScaled = true
    closeButton.Font = Enum.Font.GothamBold
    closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)

    local closeCorner = Instance.new("UICorner", closeButton)
    closeCorner.CornerRadius = UDim.new(0, 8)

    local closeStroke = Instance.new("UIStroke", closeButton)
    closeStroke.Color = Color3.fromRGB(60, 20, 20)
    closeStroke.Thickness = 1

    closeButton.MouseButton1Click:Connect(function()
        removeBlur()
        pcall(function()
            if ScreenGui then ScreenGui:Destroy() end
        end)
    end)
end

pcall(function()
    TweenService:Create(blurInstance, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = 14}):Play()
    mainFrame.Position = UDim2.new(0.5, 0, 0.53, 0)
    mainFrame.BackgroundTransparency = 1
    TweenService:Create(mainFrame, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        BackgroundTransparency = 0,
        Position = UDim2.new(0.5, 0, 0.5, 0)
    }):Play()
end)

local function destroyUI()
    pcall(function()
        TweenService:Create(blurInstance, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = 0}):Play()
        TweenService:Create(mainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            BackgroundTransparency = 1,
            Position = UDim2.new(0.5, 0, 0.53, 0)
        }):Play()
        task.wait(0.26)
        removeBlur()
        ScreenGui:Destroy()
    end)
end

local function startCountdown(seconds)
    local startTime = tick()
    local duration = math.max(1, seconds)
    while true do
        local elapsed = tick() - startTime
        local pct = math.clamp(1 - (elapsed / duration), 0, 1)
        pcall(function()
            progressInner.Size = UDim2.new(pct, 0, 1, 0)
            secondsLabel.Text = tostring(math.ceil(pct * duration))
        end)
        if pct <= 0 then break end
        task.wait(0.05)
    end
    destroyUI()
end

task.spawn(function()
    startCountdown(COUNTDOWN_SECONDS)
end)

copyButton.MouseButton1Click:Connect(function()
    pcall(function()
        if setclipboard then
            setclipboard(DISCORD_LINK)
        elseif toclipboard then
            toclipboard(DISCORD_LINK)
        elseif Clipboard and Clipboard.set then
            Clipboard.set(DISCORD_LINK)
        end
    end)
    copyButton.Text = "Copied ✓"
    task.delay(1.5, function()
        if copyButton and copyButton.Parent then
            copyButton.Text = "Copy Discord Link"
        end
    end)
end)

if player then
    player.AncestryChanged:Connect(function()
        if not player:IsDescendantOf(game) then
            removeBlur()
            pcall(function() ScreenGui:Destroy() end)
        end
    end)
end
